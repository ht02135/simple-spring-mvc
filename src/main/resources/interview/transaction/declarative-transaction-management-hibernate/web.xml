<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
            http://xmlns.jcp.org/xml/ns/javaee
            http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
         version="3.1">
         
    <!-- 
    since we already have a classic Spring (non-Spring Boot) setup 
    with applicationContext.xml and controllers, you’ll also need 
    a web.xml to bootstrap the DispatcherServlet and load Spring’s context.
	Here’s a minimal but complete web.xml for your previous example:
	/////////////////////////
	ContextLoaderListener loads the root context (applicationContext.xml) → 
	beans, services, DAOs, tx-manager.
	DispatcherServlet loads Spring MVC context (dispatcher-servlet.xml) → 
	controllers, view resolvers, web mappings.
	CharacterEncodingFilter ensures UTF-8 handling.
    -->

    <display-name>SpringHibernateApp</display-name>

    <!-- ====================== Spring Context Loader ====================== -->
    <!-- Loads applicationContext.xml (beans, services, dao, tx-manager, etc.) -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>/WEB-INF/applicationContext.xml</param-value>
    </context-param>

    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    <!-- ====================== Spring MVC Dispatcher Servlet ====================== -->
    <!-- Handles all requests and loads dispatcher-servlet.xml -->
    <servlet>
        <servlet-name>dispatcher</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>/WEB-INF/dispatcher-servlet.xml</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>dispatcher</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

    <!-- ====================== Character Encoding Filter ====================== -->
    <filter>
        <filter-name>encodingFilter</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>

    <filter-mapping>
        <filter-name>encodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

</web-app>