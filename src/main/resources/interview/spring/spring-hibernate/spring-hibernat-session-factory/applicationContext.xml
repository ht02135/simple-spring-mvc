<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx.xsd
           http://www.springframework.org/schema/aop
           http://www.springframework.org/schema/aop/spring-aop.xsd">

<!--  
3. Hibernate Session Factory Configuration (Updated applicationContext.xml)
Added configLocation property pointing to classpath:hibernate.cfg.xml
Much cleaner configuration with most properties moved to hibernate.cfg.xml
Still allows overriding specific properties (like SpringSessionContext) in hibernateProperties
////////////////////////
////////////////////////
Benefits of this approach:
Centralized Configuration: All Hibernate settings are in one place (hibernate.cfg.xml)
Consistency: Both JPA and SessionFactory use the same base configuration
Flexibility: You can still override specific properties in applicationContext.xml when needed
Maintainability: Easier to manage Hibernate properties in a single location
File Placement:
Make sure to place the hibernate.cfg.xml file in your src/main/resources directory 
so it's available on the classpath.
-->

    <!-- Component Scanning -->
    <context:component-scan base-package="com.example" />

    <!-- DataSource Configuration -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource">
        <property name="jdbcUrl" value="jdbc:mysql://localhost:3306/testdb"/>
        <property name="username" value="root"/>
        <property name="password" value="password"/>
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver"/>
        <property name="maximumPoolSize" value="10"/>
        <property name="minimumIdle" value="5"/>
        <property name="connectionTimeout" value="30000"/>
        <property name="idleTimeout" value="600000"/>
        <property name="maxLifetime" value="1800000"/>
    </bean>

    <!-- Hibernate SessionFactory with hibernate.cfg.xml reference -->
    <bean id="sessionFactory" 
          class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="packagesToScan" value="com.example.entity"/>
        <!-- Reference hibernate.cfg.xml file -->
        <property name="configLocation" value="classpath:hibernate.cfg.xml"/>
        <!-- You can still override specific properties if needed -->
        <property name="hibernateProperties">
            <props>
                <!-- These will override any conflicting properties from hibernate.cfg.xml -->
                <prop key="hibernate.current_session_context_class">org.springframework.orm.hibernate5.SpringSessionContext</prop>
            </props>
        </property>
    </bean>

    <!-- Hibernate Transaction Manager -->
    <bean id="transactionManager" 
          class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <!-- Enable Annotation-driven Transaction Management -->
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- AOP Configuration for Transaction Advice -->
    <aop:config>
        <aop:pointcut id="daoLayerPointcut" 
                      expression="execution(* com.example.dao.*.*(..))"/>
        <aop:pointcut id="serviceLayerPointcut" 
                      expression="execution(* com.example.service.*.*(..))"/>
        <aop:advisor advice-ref="txAdvice" pointcut-ref="daoLayerPointcut"/>
        <aop:advisor advice-ref="txAdvice" pointcut-ref="serviceLayerPointcut"/>
    </aop:config>

    <!-- Transaction Advice -->
    <tx:advice id="txAdvice" transaction-manager="transactionManager">
        <tx:attributes>
            <tx:method name="find*" read-only="true" propagation="SUPPORTS"/>
            <tx:method name="get*" read-only="true" propagation="SUPPORTS"/>
            <tx:method name="search*" read-only="true" propagation="SUPPORTS"/>
            <tx:method name="count*" read-only="true" propagation="SUPPORTS"/>
            <tx:method name="save*" propagation="REQUIRED"/>
            <tx:method name="update*" propagation="REQUIRED"/>
            <tx:method name="delete*" propagation="REQUIRED"/>
            <tx:method name="create*" propagation="REQUIRED"/>
            <tx:method name="*" propagation="REQUIRED"/>
        </tx:attributes>
    </tx:advice>

</beans>