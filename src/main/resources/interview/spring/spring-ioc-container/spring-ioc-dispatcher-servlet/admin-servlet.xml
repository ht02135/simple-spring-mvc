<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Admin Dispatcher Servlet Configuration -->
    
    <!-- Component scanning for admin controllers -->
    <context:component-scan base-package="com.example.controller.admin" />

    <!-- Enable Spring MVC -->
    <mvc:annotation-driven />

    <!-- Static resources for admin -->
    <mvc:resources mapping="/admin-assets/**" location="/WEB-INF/admin-resources/" />

    <!-- View resolver for admin JSP pages -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/admin/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- Multipart resolver for file uploads in admin -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="10485760" /> <!-- 10MB -->
    </bean>

    <!-- Admin-specific interceptors -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**" />
            <bean class="com.example.interceptor.AdminSecurityInterceptor" />
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/**" />
            <bean class="com.example.interceptor.AdminAuditInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>

</beans>