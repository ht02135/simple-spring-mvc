<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Web Dispatcher Servlet Configuration -->
    
    <!-- Component scanning for web controllers -->
    <context:component-scan base-package="com.example.controller.web" />

    <!-- Enable Spring MVC -->
    <mvc:annotation-driven />

    <!-- Static resources -->
    <mvc:resources mapping="/resources/**" location="/WEB-INF/resources/" />

    <!-- View resolver for JSP -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/web/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- Interceptors for web requests -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**" />
            <bean class="com.example.interceptor.WebSecurityInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>

</beans>